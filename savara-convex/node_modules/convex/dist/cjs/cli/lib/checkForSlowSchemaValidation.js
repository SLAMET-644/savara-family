"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var checkForSlowSchemaValidation_exports = {};
__export(checkForSlowSchemaValidation_exports, {
  checkForSlowSchemaValidation: () => checkForSlowSchemaValidation
});
module.exports = __toCommonJS(checkForSlowSchemaValidation_exports);
var import_chalk = require("chalk");
var import_log = require("../../bundler/log.js");
var import_deploy2 = require("./deploy2.js");
var import_utils = require("./utils/utils.js");
const MIN_BYTES_FOR_SCHEMA_WALK_WARNING = 1 << 27;
async function checkForSlowSchemaValidation({
  ctx,
  span,
  request,
  options
}) {
  const response = await evaluateSchemaBestEffort({
    ctx,
    span,
    request,
    options
  });
  if (response === null) {
    return;
  }
  const walkedTables = Object.entries(
    response.componentSchemaEvaluations
  ).flatMap(
    ([componentPath, prediction]) => prediction.tables.filter((table) => table.outcome === "mustWalk").map((table) => ({ componentPath, table }))
  );
  if (walkedTables.length === 0) {
    return;
  }
  const totalBytes = walkedTables.reduce(
    (sum, { table }) => sum + table.sizeBytes,
    0
  );
  if (totalBytes < minBytesForSchemaWalkWarning()) {
    return;
  }
  (0, import_log.logMessage)(`\u26A0\uFE0F  This schema change requires checking every document in the following ${walkedTables.length === 1 ? "table" : "tables"} against your new schema, totaling ${import_chalk.chalkStderr.yellowBright((0, import_utils.formatSize)(totalBytes))}. This deploy may take a while:

${walkedTables.map(
    ({ componentPath, table }) => formatWalkedTable({ componentPath, table })
  ).join("\n")}
`);
}
async function evaluateSchemaBestEffort({
  ctx,
  span,
  request,
  options
}) {
  try {
    return await (0, import_deploy2.evaluateSchema)(
      ctx,
      span,
      request,
      options,
      /* bestEffort */
      true
    );
  } catch (error) {
    (0, import_log.logVerbose)(
      `Skipping slow schema validation check: ${error instanceof Error ? error.message : String(error)}`
    );
    return null;
  }
}
function formatWalkedTable({
  componentPath,
  table
}) {
  const componentPrefix = componentPath !== "" ? `${import_chalk.chalkStderr.gray(componentPath)}: ` : "";
  const docsFormatted = `${table.numDocs.toLocaleString()} documents, `;
  const sizeFormatted = (0, import_utils.formatSize)(table.sizeBytes);
  return `  ${componentPrefix}${table.name} (${docsFormatted}${sizeFormatted})`;
}
function minBytesForSchemaWalkWarning() {
  const envValue = process.env.CONVEX_MIN_BYTES_FOR_SCHEMA_WALK_WARNING;
  if (envValue !== void 0) {
    const parsed = parseInt(envValue, 10);
    if (!isNaN(parsed)) {
      return parsed;
    }
  }
  return MIN_BYTES_FOR_SCHEMA_WALK_WARNING;
}
//# sourceMappingURL=checkForSlowSchemaValidation.js.map
